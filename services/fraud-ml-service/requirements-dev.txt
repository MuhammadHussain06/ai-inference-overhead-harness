# Test-only dependencies. Not installed into the service image -- the Dockerfile
# builds from requirements.txt so test tooling stays off the measured container.
-r requirements.txt
pytest==8.3.3
httpx==0.27.2